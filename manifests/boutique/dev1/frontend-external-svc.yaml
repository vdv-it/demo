apiVersion: v1
kind: Service
metadata:
  name: frontend-external
  annotations:
    external-dns.alpha.kubernetes.io/hostname: dev1.dev.zbsnu.click 
spec:
  type: LoadBalancer
  selector:
    app: frontend
  ports:
  - name: http
    port: 80
    targetPort: 8080
    