apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: voting-applicationset
  namespace: argocd
spec:
  generators:
  - list:
      elements:
      - namespace: dev-1
      - namespace: dev-2
      - namespace: dev-3
  template:
    metadata:
      name: '{{namespace}}-voting-app'
    spec:
      project: default
      source:
        repoURL: https://github.com/kuttleio/k8s-apps.git
        targetRevision: HEAD
        path: voting-app
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{namespace}}'
        